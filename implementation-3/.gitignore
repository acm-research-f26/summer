# Python
__pycache__/
*.py[cod]
*.egg-info/

# generated by pip install -e framework/
framework/verl.egg-info/

# Hydra run logs from launching the trainer
framework/outputs/

# model/training artifacts
*.pt
*.pth
*.ckpt
wandb/
checkpoints/

# research/ run outputs (generated, not source)
research/baseline_a_results.json
research/baseline_b_results.json
research/seal_round_timings.jsonl
research/forgetting_curve.json
research/seal_comparison.json

# runs/ per-run results (generated, not source) -- EXPERIMENT_LOG.md is the
# tracked cumulative summary; the raw per-run JSON/JSONL underneath is not.
runs/*/results/

# misc
*.log
*.tmp
.DS_Store

# internal working docs -- not part of the final submission. README.md is the
# submission-facing summary; these are session-log-flavored internal notes
# (deep technical reference, append-only run log, ad hoc results scratch
# notes) kept locally for our own reference but not meant for a reviewer.
PROJECT_GUIDE.md
EXPERIMENT_LOG.md
runs/paper_scale/RESULTS.md
