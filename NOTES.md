Columns to look into dropping are RFR and CFR from the WRI dataset.

Of these 271 columns, figure out which ones are the most useful for training a machine learning model. Ideally, we want to reduce the feature set to only include the most impactful labels and indicators.